import React, { Component, PropTypes } from 'react';
import './button.css';

class Button extends Component { 

    render() {
        return (<button className="startGame">Start Game</button>);
    }
}

export default Button;
